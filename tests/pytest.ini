[pytest]
 # --setup-show - Показать что и когда выполняется
 #  --debug - вывод отладочно йнформации в pytestdebug.log
 # --log-file=nitis_sukdtdt_pytest.log --log-file-level=DEBUG
addopts = -rsxX -l -v --tb=short --strict
norecursedirs = .* venv src *.egg dist build app
;testpaths = ./tests